// Constructor function for the basic card
function BasicCard(front, back) {
        this.front = front;
        this.back = back;

};

// Exports the basic card constructor
module.exports = BasicCard;